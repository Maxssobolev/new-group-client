import styled from 'styled-components';

const Block = styled.div`
  background-color: var(--white);
  box-shadow: var(--sc-box-shadow);
  border-radius: 15px;
  padding: 40px 50px;
`;

export const SCInvitePage = { Block };
